{
  "name": "api",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "json-server --watch db.json --port 3001",
    "start": "json-server db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}

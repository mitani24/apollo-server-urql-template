query track($trackId: ID!) {
  track(id: $trackId) {
    id
    title
    author {
      id
      name
      photo
    }
    thumbnail
    description
    length
    modulesCount
    description
    numberOfViews
    modules
  }
}
